### PovFuzzing

fuzzes for pov's!


# need to install
sudo apt-get install python-pip libpq-dev


# need to include a dot env file
python-dotenv
compilerex
farnworth
pov_fuzzing

# logging?
standarg_logging.py
